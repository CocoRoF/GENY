# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
httpx>=0.28.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# numpy stays for image / waveform / statistics; FAISS removed.
numpy>=1.26.0
# File pattern matching
pathspec>=0.12.0
# PostgreSQL database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# Web search
ddgs==9.11.4
# Web fetch & browser automation
playwright>=1.49.0
# geny-executor 1.19.0 ships LoopAgnosticLock (cross-loop safe memory
# store locks — fixes silent write swallowing under run_coro_sync) and
# consolidates extras into baseline deps (web/cron/openai/google/postgres
# all installed by default; embedding provider chosen at runtime via
# settings.json).
geny-executor>=1.19.0,<2.0.0
# MCP (Model Context Protocol)
mcp>=1.0.0
# TTS (Text-to-Speech)
edge-tts>=6.1
# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
