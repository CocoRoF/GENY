# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
httpx>=0.28.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# numpy stays for image / waveform / statistics; FAISS removed.
numpy>=1.26.0
# File pattern matching
pathspec>=0.12.0
# PostgreSQL database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# Web search
ddgs==9.11.4
# Web fetch & browser automation
playwright>=1.49.0
# geny-executor 2.1.1 — Anthropic API boundary robustness:
# AnthropicClient expands ``opus``/``sonnet``/``haiku`` aliases to
# canonical model IDs before the SDK call, and drops temperature /
# top_p / top_k when ``thinking`` is set (the Messages API rejects
# them under thinking with 400). CLI surface unchanged.
#
# Must match the version pin in backend/pyproject.toml — out-of-sync
# pins are what caused the prod Docker build to install 1.21.0 and
# blow up on `from geny_executor import CredentialBundle`.
geny-executor>=2.1.1,<3.0.0
# MCP (Model Context Protocol)
mcp>=1.0.0
# TTS (Text-to-Speech)
edge-tts>=6.1
# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
