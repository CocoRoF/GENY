# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
httpx>=0.28.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# FAISS still required by the legacy service.memory.vector_store path
# during the cut-over window. Dropped in the follow-up PR once every
# caller has been ported.
faiss-cpu>=1.9.0
numpy>=1.26.0
# File pattern matching
pathspec>=0.12.0
# PostgreSQL database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# Web search
ddgs==9.11.4
# Web fetch & browser automation
playwright>=1.49.0
# 1.16.0 ships the unified MemoryProvider with composite-layer
# CuratedHandle/GlobalHandle wrappers plus auto-vector on note write.
# `[openai]` is required because Geny's LTM settings default to the
# OpenAI embedding backend.
geny-executor[web,cron,openai]>=1.16.0,<2.0.0
# MCP (Model Context Protocol)
mcp>=1.0.0
# TTS (Text-to-Speech)
edge-tts>=6.1
# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
