# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
httpx>=0.28.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# Vector search
faiss-cpu>=1.9.0
numpy>=1.26.0
# File pattern matching
pathspec>=0.12.0
# PostgreSQL database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# Web search
ddgs==9.11.4
# Web fetch & browser automation
playwright>=1.49.0
# Bumped to 1.3.0 for new-executor-uplift Cycle A+B+D (Task lifecycle /
# Slash commands / Cron / settings.json / SKILL.md richer / PLAN modes /
# Workspace abstraction). cron extra pulls croniter for the scheduling
# daemon. Pin matches backend/pyproject.toml so docker layer cache and
# pip lockstep agree on the resolved version.
geny-executor[web,cron]>=1.8.0,<2.0.0
# MCP (Model Context Protocol)
mcp>=1.0.0
# TTS (Text-to-Speech)
edge-tts>=6.1
# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
