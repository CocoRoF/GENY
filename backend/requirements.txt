# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
httpx>=0.28.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# numpy stays for image / waveform / statistics; FAISS removed.
numpy>=1.26.0
# File pattern matching
pathspec>=0.12.0
# PostgreSQL database
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
# Web search
ddgs==9.11.4
# Web fetch & browser automation
playwright>=1.49.0
# geny-executor 2.1.4 — Claude Code CLI 2.1.x token-level streaming.
# The CLI emits per-token deltas inside ``{"type":"stream_event"}``
# wrappers under ``--include-partial-messages``; 2.1.3 only knew the
# legacy delta + full-envelope shapes, so every Geny chat showed the
# whole answer in one chunk at the end. 2.1.4 teaches the parser the
# new shape and de-duplicates the terminal envelope that arrives
# after the per-token deltas.
#
# Must match the version pin in backend/pyproject.toml — out-of-sync
# pins are what caused the prod Docker build to install 1.21.0 and
# blow up on `from geny_executor import CredentialBundle`.
geny-executor>=2.1.4,<3.0.0
# MCP (Model Context Protocol)
mcp>=1.0.0
# TTS (Text-to-Speech)
edge-tts>=6.1
# Auth
bcrypt>=4.0.0
PyJWT>=2.8.0
